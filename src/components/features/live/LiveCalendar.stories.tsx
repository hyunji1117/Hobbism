// LiveCalendar.stories.tsx

import { LiveCalendar } from './LiveCalendar';
import React from 'react';

const meta = {
  title: 'Components/LiveCalendar',
  component: LiveCalendar,
};

export default meta;

export const Default = {
  render: () => <LiveCalendar />,
};
